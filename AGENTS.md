# 项目可维护性与 AI 开发规范

本文档是本项目的共同开发约定，适用于人类开发者和 AI 编程助手。目标是让代码易于理解、修改、验证、部署和交接。

## 1. 总体原则

- 优先选择简单、直接、可读的实现，不为“看起来高级”而引入抽象或设计模式。
- 以降低未来变更成本为判断标准，而不是以代码行数或规则数量为目标。
- 一个模块只承担一类主要职责；一个函数只处理一个主要任务。
- 业务规则应有唯一来源，避免复制粘贴和多处实现同一规则。
- 明确处理错误，不得静默吞掉异常、返回无依据的默认值或隐藏失败。
- 保持公共 API、数据格式和既有行为的兼容性；破坏性变更必须说明迁移方案。

## 2. AI 修改代码的工作流程

AI 在开始修改前必须阅读相关目录、入口文件、类型定义、配置、测试和文档，并说明架构理解、影响范围、风险和计划。

实现时只做当前任务所需的最小修改，保持依赖方向，不擅自改变公共 API、数据库结构、配置格式或用户可见行为。超过约 300 行或职责过多的文件，先记录拆分评估和迁移计划。

完成后必须分别列出生产代码、测试、文档和配置变更，并运行适用的格式化、静态检查、类型检查和测试命令；无法执行时说明原因和剩余风险。

## 3. 目录和依赖边界

本项目按 Android 技术栈调整边界：

- `app/src/main`: Android interface/application 层，包括 Compose UI、生命周期和前台服务。
- `ts3-protocol/src/main`: 协议 domain/application 适配层；纯 JVM 模型和协议端口不依赖 Android。
- `audio-opus/src/main`: 音频 infrastructure 适配层，包括 JNI、设备路由和音频 I/O。
- 各模块的 `src/test` 和 `src/androidTest`: 测试、fixture 和设备验证代码。
- `docs/`: 架构说明、决策记录和发布资料。

依赖方向应尽量保持 `interface -> application -> domain`，基础设施通过接口接入；不得引入无记录的全局可变状态。

## 4. 测试隔离规则

- 生产代码和测试代码必须物理分离；生产文件中禁止出现测试框架导入或测试专用分支。
- 每个生产模块对应独立测试文件；新行为必须有测试，Bug 修复必须有回归测试。
- 测试不得依赖执行顺序、开发者本机环境或真实线上服务。
- 网络协议测试使用 fixture、fake 或 mock server，并避免提交真实服务器地址、身份和密码。

## 5. 代码可读性和错误处理

- 使用表达业务含义的命名，避免模糊的 `data`、`info`、`handle`。
- 复杂逻辑拆成职责明确、可独立测试的函数。
- 区分输入错误、业务冲突、外部服务失败和系统错误，并在边界转换错误。
- 日志不得泄露密码、令牌、个人隐私、完整服务器地址或完整协议请求。

## 6. API、数据库和配置变更

- 公共 API 变更必须说明兼容性影响和迁移方案。
- 本项目当前没有数据库；未来如引入数据库必须使用可审计 migration。
- 配置项必须有 `local.properties.example` 或等价说明；不得把密钥写入代码、测试或提交记录。

## 7. 文档要求

至少维护：`README.md`、`ARCHITECTURE.md`、`CONTRIBUTING.md`、`docs/decisions/` 和配置示例文件。代码、配置或部署方式变化时同步更新文档。

## 8. 合并前质量门禁

提交或合并前必须通过适用的格式化、静态检查、类型检查、单元/集成测试、安全检查和文档同步检查。覆盖率用于发现盲区，不以 100% 为绝对目标。

## 9. AI 任务模板

每次 AI 任务应先阅读相关代码并说明理解、影响范围和计划，再修改生产代码、补充独立测试、运行格式化/Lint/类型检查/测试，最后报告修改文件、结果和未覆盖风险。
